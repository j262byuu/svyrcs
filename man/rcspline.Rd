% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis.R
\name{rcspline}
\alias{rcspline}
\title{Restricted cubic spline basis}
\usage{
rcspline(x, knots = 4)
}
\arguments{
\item{x}{Numeric exposure vector.}

\item{knots}{Either the number of knots (3 to 7, default 4), placed at Harrell's recommended
quantiles of \code{x}, or an explicit numeric vector of knot locations. \code{\link[=svyrcs]{svyrcs()}} normally replaces
a count with survey-weighted quantiles before fitting.}
}
\value{
A numeric matrix with \code{length(x)} rows and \code{k - 1} columns, of class \code{svyrcs_basis}, with
attributes \code{knots} and \code{nk}.
}
\description{
Builds a restricted cubic spline (natural spline) basis for use inside a model formula. The basis
is Harrell's truncated power parameterisation and matches \code{rms::rcs()} -- both the basis and, since
0.6.2, the knot placement this function performs, including the small-sample rule that moves the
outer knots to
the fifth smallest and fifth largest values below 100 observations. Unlike \code{rms}, it stores its
knots on the returned object and registers a \code{\link[stats:makepredictcall]{stats::makepredictcall()}} method, so a model fitted
with \code{rcspline()} reuses exactly the same knots when predicting on new data.
}
\details{
Because a restricted cubic spline with \code{k} knots contributes \code{k - 1} columns, the first of which
is \code{x} itself, a test that the remaining columns are jointly zero is a test of non-linearity. This
is what \code{\link[=svyrcs]{svyrcs()}} reports as the non-linearity \emph{p}-value.
}
\section{Relationship to \code{rms}}{

This function used to be called \code{rcs()}, which masked \code{rms::rcs()} whenever both packages were
attached. It was renamed in 0.7.0 because masking could not be made safe: an \code{ols()}, \code{lrm()} or
\code{cph()} model written with a bare \code{rcs()} would pick up this basis, fit correctly, and then lose
the \code{Nonlinear} row from \code{anova()} with no error and no warning -- and that row is usually the
reason for fitting a spline at all.

With the rename there is no collision. \code{rcs()} in an \code{rms} formula is unambiguously \code{rms::rcs()}.

The basis itself is Harrell's, and agrees with \code{rms::rcs()} to about 1e-14 \strong{given the same
knots}. Note the converse still holds: passing \code{rcspline()} into an \code{rms} model fits, but the
result is not an \code{rms} design term, so \code{anova()} reports no \code{Nonlinear} row and \code{Predict()} fails.
Use \code{rms::rcs()} for \code{rms} models and \code{rcspline()} for this package.
}

\examples{
b <- svyrcs::rcspline(nhanes_bmi$bmi, 4)
dim(b)
attr(b, "knots")

}
\seealso{
\code{\link[=svyrcs]{svyrcs()}}, \code{\link[=rcs_knots]{rcs_knots()}}, and \code{rms::rcs()} for \code{rms} models -- the two are not
interchangeable in an \code{rms} formula, see the section above.
}
