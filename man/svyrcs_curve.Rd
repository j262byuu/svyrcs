% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve.R
\name{svyrcs_curve}
\alias{svyrcs_curve}
\title{Exposure-response curve from a fitted survey model}
\usage{
svyrcs_curve(
  fit,
  var = NULL,
  ref = "median",
  ref_prob = 0.5,
  at = NULL,
  n = 200,
  range = NULL,
  level = 0.95,
  design = NULL,
  degf = NULL,
  group = NULL,
  df = NULL
)
}
\arguments{
\item{fit}{A fitted model containing an \code{\link[=rcspline]{rcspline()}} term, typically from \code{\link[survey:svycoxph]{survey::svycoxph()}} or
\code{\link[survey:svyglm]{survey::svyglm()}}. Any model with \code{coef()}, \code{vcov()} and \code{terms()} methods works.}

\item{var}{Name of the exposure. Only needed when the model contains more than one \code{rcspline()} term.}

\item{ref}{Reference value: a number, or one of \code{"median"}, \code{"mean"}, \code{"quantile"}, \code{"min"}
(minimum-risk point) or \code{"max"} (maximum-risk point). See \code{\link[=svyrcs]{svyrcs()}}.}

\item{ref_prob}{Probability used when \code{ref = "quantile"}.}

\item{at}{Optional numeric vector of exposure values to evaluate. When given, \code{n} and \code{range} are
ignored and the result has one row per element of \code{at}.}

\item{n}{Number of points on the curve.}

\item{range}{Length-2 numeric giving the exposure range to plot over. Defaults to the 1st and 99th
survey-weighted percentiles of the exposure.}

\item{level}{Confidence level.}

\item{design}{The survey design. Defaults to the design stored on \code{fit}, which is what
\code{\link[survey:svyglm]{survey::svyglm()}} and \code{\link[survey:svycoxph]{survey::svycoxph()}} keep. Needed for weighted reference values and
ranges.}

\item{degf}{Design degrees of freedom for the \emph{t} quantile. Defaults to \code{survey::degf(design)}.}

\item{group}{When the spline is interacted with an effect modifier, the level or levels to
estimate. \code{NULL} (default) returns every level, stacked, with a \code{group} column.}

\item{df}{Denominator degrees of freedom to use instead of the design's. \code{NULL} (default) keeps
\code{survey::degf(design)}; a number substitutes it; \code{Inf} gives normal-quantile intervals. See the
note in \code{\link[=svyrcs]{svyrcs()}} on why this is a policy rather than a fixed answer.}
}
\value{
A data frame of class \code{svyrcs_curve} with columns \code{x}, \code{estimate}, \code{conf.low},
\code{conf.high} and \code{se} (on the linear-predictor scale), plus \code{group} when the model has an
effect modifier. Carries attributes \code{ref}, \code{ref_method}, \code{measure}, \code{null}, \code{knots}, \code{var},
\code{degf}, \code{level} and \code{modifier}.
}
\description{
Estimates the exposure-response curve implied by an \code{rcspline()} term in an already-fitted survey
model, as a contrast against a reference exposure value. \code{\link[=svyrcs]{svyrcs()}} calls this internally; use it
directly when you have fitted the model yourself, for instance because you need a model
specification \code{svyrcs()} does not build for you.
}
\details{
The curve is computed from the spline coefficients alone. Every covariate takes the same value at
\code{x} and at the reference, so covariate contributions cancel exactly and no covariate reference
grid is needed. Confidence intervals use a \emph{t} quantile on the survey degrees of freedom.
}
\examples{
design <- survey::svydesign(
  id = ~psu, strata = ~strata, weights = ~weight,
  nest = TRUE, data = nhanes_bmi
)
fit <- survey::svyglm(tchol ~ svyrcs::rcspline(bmi, 4) + age + sex, design = design)
curve <- svyrcs_curve(fit, "bmi", n = 20)
head(curve)

}
\seealso{
\code{\link[=svyrcs]{svyrcs()}}
}
