% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svyrcs.R
\name{svyrcs}
\alias{svyrcs}
\title{Restricted cubic spline analysis of complex survey data}
\usage{
svyrcs(
  formula,
  design,
  family = NULL,
  ref = "median",
  ref_prob = 0.5,
  weighted_knots = TRUE,
  n = 200,
  range = NULL,
  level = 0.95,
  df = NULL,
  ...
)
}
\arguments{
\item{formula}{A model formula containing exactly one \code{\link[=rcs]{rcs()}} term for the exposure, plus any
covariates. A \code{Surv()} response selects a survey-weighted Cox model; anything else is fitted
with \code{\link[survey:svyglm]{survey::svyglm()}}. Crossing the spline with a grouping variable, \code{rcs(x, 4) * sex},
estimates one curve per group and tests whether they differ; see the section below.}

\item{design}{A survey design object, as built by \code{\link[survey:svydesign]{survey::svydesign()}}. Subset it with
\code{\link[=subset]{subset()}} before passing it in; the degrees of freedom are taken from the design you pass.
A multiply imputed design, built by passing a \code{mitools::imputationList()} to
\code{\link[survey:svydesign]{survey::svydesign()}}, is also accepted; see the section below.}

\item{family}{Family for the \code{\link[survey:svyglm]{survey::svyglm()}} path. Use \code{quasibinomial()} for a binary outcome
(odds ratios), \code{quasipoisson()} for counts or rates (rate ratios), or the default
\code{gaussian()} for a continuous outcome (mean differences). Ignored for Cox models.}

\item{ref}{Reference value the curve is anchored to: a number, or one of \code{"median"} (default),
\code{"mean"}, \code{"quantile"}, \code{"min"} or \code{"max"}. See \link{references}.}

\item{ref_prob}{Probability used when \code{ref = "quantile"}.}

\item{weighted_knots}{If \code{TRUE} (default), a knot \emph{count} is turned into knot locations using
survey-weighted quantiles of the exposure, so knot placement reflects the population rather than
the unweighted sample. Set to \code{FALSE} for unweighted quantiles. Explicit knot locations are
always used as given.}

\item{n}{Number of points on the estimated curve.}

\item{range}{Length-2 numeric giving the exposure range to estimate over. Defaults to the 1st and
99th survey-weighted percentiles.}

\item{level}{Confidence level for the band.}

\item{df}{Denominator degrees of freedom for the confidence band and the tests. \code{NULL} (default)
uses \code{survey::degf(design)}; a number substitutes it; \code{Inf} gives normal-quantile intervals and
chi-square-equivalent tests. Under multiple imputation this is the complete-data degrees of
freedom that the Barnard-Rubin correction starts from.}

\item{...}{Passed on to \code{\link[survey:svycoxph]{survey::svycoxph()}} or \code{\link[survey:svyglm]{survey::svyglm()}}.}
}
\value{
An object of class \code{svyrcs}, a list with components:
\describe{
\item{curve}{Data frame of \code{x}, \code{estimate}, \code{conf.low}, \code{conf.high}, \code{se}.}
\item{ref}{The reference value and how it was chosen.}
\item{knots}{Knot locations actually used.}
\item{tests}{Overall and non-linearity Wald tests, as \emph{F} and chi-square.}
\item{measure}{\code{"HR"}, \code{"OR"}, \code{"RR"} or \code{"Difference"}.}
\item{model}{The fitted \code{svycoxph}/\code{svyglm} object, for your own diagnostics.}
\item{degf, n, nevents, level, var, call}{Analysis metadata.}
}
}
\description{
Fits a restricted cubic spline model to a complex survey design and returns the
exposure-response curve, its confidence band, tests of overall association and of non-linearity,
and the reference value the curve is anchored to.
}
\section{Subgroups and effect modification}{

Writing the exposure crossed with a grouping variable, \code{rcs(bmi, 4) * sex}, fits \strong{one} model and
returns one curve per level of the modifier. Because it is a single model, the covariate effects
are shared across groups and a genuine interaction test is available; fitting each subgroup
separately gives neither. \code{sex * rcs(bmi, 4)} is equivalent.

The modifier must be a factor, character or logical variable; ordered factors and other contrast
codings are handled, because group curves are built from the contrast matrix rather than from
level names. Two extra tests are then reported:
\describe{
\item{Interaction}{Are the spline-by-group terms jointly zero, i.e. does the association differ
between groups at all?}
\item{Shape interaction}{The same test without the linear term: does the \emph{curvature} differ, as
opposed to the whole curve being shifted?}
}
plus the overall and non-linearity tests within each group.

All groups share one reference value, so that the curves are comparable; \code{ref = "min"} and
\code{ref = "max"} are located on the reference level's curve, which the printed output states.
}

\section{What is warned about}{

Some situations are legitimate but easy to enter by accident, so they warn rather than fail:
evaluating the curve outside the observed exposure range, which a restricted cubic spline will do
silently because it is linear beyond the outer knots; a design whose degrees of freedom cannot
identify the spline, where the estimates stand but the intervals do not; and non-finite estimates
from a model that did not converge. A reference value outside the observed range is refused
outright instead, because it would make every estimate an extrapolation rather than the one point
asked for.
}

\section{Size of the returned object}{

\code{fit$model} keeps the fitted model, which keeps the survey design, which keeps the data. A fit on
the shipped 10,617-row design is about 9 MB, and a multiply imputed one is that again per
imputation -- roughly 47 MB at \code{m = 5}. That is the price of being able to run your own
diagnostics on \code{fit$model}; drop it before saving a large batch of fits if size matters.
}

\section{Multiply imputed designs}{

Pass a design built from a \code{mitools::imputationList()} and the model is fitted in every
imputation and combined by Rubin's rules. Knots and the reference value are computed \strong{once}, as
the average of the survey-weighted quantiles across imputations, and then held fixed: if each
imputation chose its own knots, the coefficient vectors would not be estimates of the same
parameters and Rubin's rules would not apply.

The curve then carries two extra columns, \code{df} and \code{fmi}: the degrees of freedom used at that
point and the fraction of missing information there.

\strong{On the degrees of freedom.} \code{mitools::MIcombine()} reports
\eqn{(m - 1)(1 + 1/r)^2}, which is unbounded above — against the design shipped with this package,
which has 31 degrees of freedom, it returns values in the thousands or \code{Inf}. Using that for a
\emph{t} quantile would give confidence intervals far too narrow. \code{svyrcs} applies the Barnard-Rubin
correction instead, which bounds the result by the complete-data degrees of freedom and returns
exactly \code{degf(design)} when there is nothing to impute. The same reasoning applies to the joint
tests, whose denominator degrees of freedom are adjusted the same way rather than taken from the
Li-Raghunathan-Rubin rule, which ignores the survey design entirely.

Building the imputations is not this package's job — use \code{mice} or similar, then hand the
completed datasets over.
}

\section{Which degrees of freedom}{

By default the confidence band and the tests use \code{survey::degf(design)}, the number of primary
sampling units minus the number of strata. That is a deliberate conservative default rather than
the only defensible answer: it behaves as though every covariate varied only between PSUs, which
is right for design variables and cautious for individual-level ones. \code{survey::regTermTest()}
exposes the same choice for the same reason.

\code{df} overrides it. A larger value, or \code{Inf} for the normal approximation, gives narrower
intervals; use it deliberately and say so in the write-up, because with few PSUs the difference is
not small.
}

\section{Why not just use \code{rms}}{

\code{rms::rcs()} gives the same basis, but \code{rms} fitting functions do not know about sampling weights,
clustering or stratification, and \code{survey} has no spline helper. \code{svyrcs} bridges the two: the
spline is fitted inside a \code{survey} model, so point estimates use the weights and the confidence
band uses the variance estimator the design implies -- Taylor linearisation for an ordinary
design, replicate-weight variance for a replicate design.
}

\examples{
design <- survey::svydesign(
  id = ~psu, strata = ~strata, weights = ~weight,
  nest = TRUE, data = nhanes_bmi
)

# continuous outcome: mean difference in total cholesterol across the BMI range
fit <- svyrcs(tchol ~ rcs(bmi, 4) + age + sex, design = design)
fit

# survival outcome: all-cause mortality, anchored at the minimum-risk BMI
\donttest{
library(survival)
fit_hr <- svyrcs(Surv(time, event) ~ rcs(bmi, 4) + age + sex,
                 design = design, ref = "min")
summary(fit_hr)
plot(fit_hr)

# binary outcome: odds of high total cholesterol
fit_or <- svyrcs(high_chol ~ rcs(bmi, 4) + age + sex, design = design,
                 family = quasibinomial())

# one curve per subgroup, with a test of whether they differ
fit_by_sex <- svyrcs(Surv(time, event) ~ rcs(bmi, 4) * sex + age, design = design)
fit_by_sex$tests$interaction$p_F
plot(fit_by_sex)
}

}
\seealso{
\code{\link[=svyrcs_curve]{svyrcs_curve()}} for curves from a model you fitted yourself, \code{\link[=rcs]{rcs()}} for the basis,
\link{references} for reference values.
}
