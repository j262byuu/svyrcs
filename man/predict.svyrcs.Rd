% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.svyrcs}
\alias{predict.svyrcs}
\title{Effect estimates at specific exposure values}
\usage{
\method{predict}{svyrcs}(object, x, ref = NULL, level = NULL, group = NULL, ...)
}
\arguments{
\item{object}{An object from \code{\link[=svyrcs]{svyrcs()}}.}

\item{x}{Numeric vector of exposure values.}

\item{ref}{Reference value to contrast against. Defaults to the reference the fit was anchored to;
supply a number to compare against a different value without refitting.}

\item{level}{Confidence level. Defaults to the level used by the fit.}

\item{group}{For a fit with an effect modifier, the level or levels wanted. \code{NULL} (default)
returns every level.}

\item{...}{Ignored.}
}
\value{
A data frame with columns \code{x}, \code{estimate}, \code{conf.low}, \code{conf.high} and \code{se}, plus \code{group}
when the fit has an effect modifier.
}
\description{
Gives the estimated effect, with confidence interval, at exposure values you choose, rather than
on the plotting grid. This is what you want when a paper reports, say, the hazard ratio at a BMI
of 30 against a BMI of 25.
}
\examples{
design <- survey::svydesign(
  id = ~psu, strata = ~strata, weights = ~weight,
  nest = TRUE, data = nhanes_bmi
)
fit <- svyrcs(tchol ~ svyrcs::rcspline(bmi, 4) + age + sex, design = design)

# against the fitted reference
predict(fit, x = c(20, 25, 30, 35))

# against a BMI of 25 instead
predict(fit, x = c(20, 30, 35), ref = 25)

}
